import 'package:flutter/material.dart';

class GarbageBinScreen extends StatelessWidget {
  const GarbageBinScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Garbage Bin Screen'),
    );
  }
}
